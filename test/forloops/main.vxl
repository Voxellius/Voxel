for (var i = 0; i < 5; i++) {
    syscall log(i);
    syscall log("\n");
}

syscall log("\n");

for (var y = 0; y < 4; y++) {
    for (var x = 0; x < 4; x++) {
        syscall log("x: ");
        syscall log(x);
        syscall log(", y: ");
        syscall log(y);
        syscall log("\n");
    }
}

syscall log("\n");

var a = 0;
var b = 10;

for (; a < b; a++) {
    syscall log(a);
    syscall log("\n");

    b = b - 1;
}