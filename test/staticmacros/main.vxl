var used = 0;
var unused = 0;
var object = {};

used;

object.usedProp = 123;
object.unusedProp = 456;

object.usedProp;

syscall io_out("#used(used): ");
syscall io_out(#used(used));
syscall io_out("\n");

syscall io_out("#used(unused): ");
syscall io_out(#used(unused));
syscall io_out("\n");

syscall io_out("#usedprop(usedProp): ");
syscall io_out(#usedprop(usedProp));
syscall io_out("\n");

syscall io_out("#usedprop(unusedProp): ");
syscall io_out(#usedprop(unusedProp));
syscall io_out("\n");